# Platform facts

This file lists each platform fact the skill relies on, what depends on it, its source,
and the date it was checked; when a fact changes, retire or rewrite the rule that depends
on it in the same change.

| Fact | Depends on it | Source | Checked |
| --- | --- | --- | --- |
| Claude Code v2.1.277 and later reads `AGENTS.md` directly, at the root and in subdirectories, when no `CLAUDE.md`, `.claude/CLAUDE.md`, or `CLAUDE.local.md` exists in or above the working directory. The user's `~/.claude/CLAUDE.md`, a managed `CLAUDE.md`, and `.claude/rules/` files do not count. | Adapter policy: no adapter by default. | https://code.claude.com/docs/en/memory | 2026-09-23 |
| When any such `CLAUDE.md` exists, Claude Code reads `CLAUDE.md` files only, and a nested `AGENTS.md` loads only through a sibling `CLAUDE.md`. | Adapter policy: adapters are all or nothing. | https://code.claude.com/docs/en/memory | 2026-09-23 |
| Claude Code sessions that cannot read `AGENTS.md` directly: versions before v2.1.277, Amazon Bedrock, telemetry disabled, the built-in `agents-md` plugin disabled, or **Project instructions** set to `claude-md`. | Adapter policy: when adapters are needed. | https://code.claude.com/docs/en/memory | 2026-09-23 |
| On Windows, Git checks out a committed symlink as a plain text file unless `core.symlinks` is enabled. | Adapter policy: an adapter is never a symlink. | https://code.claude.com/docs/en/memory | 2026-09-23 |
| The **Project instructions** setting (`claude-md-or-agents-md`, `claude-md-and-agents-md`, `claude-md`, `managed-only`) is a user-level setting; project and local settings files ignore it. | Adapter policy: when adapters are needed, and why a repo cannot rely on the setting; session verification. | https://code.claude.com/docs/en/memory | 2026-09-23 |
| `@path` imports expand at load time, relative to the importing file, up to four hops deep, including inside an `AGENTS.md` that Claude Code reads directly. They are a Claude Code mechanism; Codex's `AGENTS.md` page documents no import syntax. | Pointer grammar: no leading `@` on a pointer path. Adapter content: the one import line. | https://code.claude.com/docs/en/memory | 2026-09-23 |
| A subdirectory's `CLAUDE.md` or `AGENTS.md` loads when Claude Code reads a file in that directory. | Directory spokes. | https://code.claude.com/docs/en/memory | 2026-09-23 |
| Block-level HTML comments in `CLAUDE.md` are stripped before the file is added to context. | Nothing; noted for maintainers. | https://code.claude.com/docs/en/memory | 2026-09-23 |
| Claude Code does not read `AGENTS.override.md`, `AGENTS.local.md`, or anything under `.agents/`. | Structure: `AGENTS.override.md` and `AGENTS.local.md` are not used. | https://code.claude.com/docs/en/memory | 2026-09-23 |
| Claude Code's guidance is to keep each instruction file under 200 lines. | Judgment check: size. | https://code.claude.com/docs/en/memory | 2026-09-23 |
| `.claude/rules/` is read by Claude Code only, and `paths:` frontmatter scopes a rule to matching files. | Structure: `.claude/rules/` holds only Claude-only mechanics. | https://code.claude.com/docs/en/memory | 2026-09-23 |
| Claude Code runs plugin hooks from `hooks/hooks.json`; a `PreToolUse` matcher matches the tool name, such as `Bash`, and the event's `tool_input.command` holds the command; `hookSpecificOutput.additionalContext` reaches the model without blocking the call, while plain stdout goes only to the debug log; the hook shell is bash, or PowerShell on Windows when Git Bash is absent; `CLAUDE_PLUGIN_ROOT` is set. | The pre-commit reminder hook. | https://code.claude.com/docs/en/hooks | 2026-09-26 |
| Codex builds its instruction chain once, at session start, loading at most one instruction file per directory from the repo root down to the working directory, preferring `AGENTS.override.md` over `AGENTS.md`, and stops adding files at `project_doc_max_bytes` (32 KiB by default). | Directory spokes on Codex: the hub pointer is the route. Structure: `AGENTS.override.md` is not used. Judgment check: size. | https://learn.chatgpt.com/docs/agent-configuration/agents-md | 2026-09-23 |
| Codex reads repository skills from `.agents/skills`. | Structure: `.agents/` is outside the hub and spoke contract. | https://learn.chatgpt.com/docs/build-skills | 2026-09-23 |
| Codex runs plugin hooks from `hooks/hooks.json`, supports `PreToolUse` with a matcher on the tool name (`Bash` for shell commands) and no command filter, puts the command in `tool_input.command`, passes `hookSpecificOutput.additionalContext` to the model, supplies `PLUGIN_ROOT` and `CLAUDE_PLUGIN_ROOT`, and requires the user to trust each plugin hook through `/hooks` before it runs. | The pre-commit reminder hook: the script filters for `git commit` itself; Codex users must trust it. | https://learn.chatgpt.com/docs/hooks | 2026-09-26 |
